"""EvolveStudio: a workbench on top of OpenEvolve.

Takes a natural-language problem description, compiles it into an
OpenEvolve experiment (initial program + evaluator + config), runs it,
and visualizes candidate lineage, scores, diffs, and failures.
"""

__version__ = "0.0.1"
